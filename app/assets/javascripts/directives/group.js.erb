var module = angular.module("Group.directives", []);

module.directive("userGroup", function(){
  return {
    restrict: "A",
    replace: false,
    controller: groupCtrl,
    link: function(scope, elt, attrs, controller) {
      scope.fetch();
    }
  };
});

module.directive("groupUser", function(){
 return {
    restrict: "A",
    replace: false,
    controller: groupCtrl,
    templateUrl: "<%= asset_path 'group/group_users.html' %>",
    link: function(scope, elt, attrs, controller) {

    }
  };
});
